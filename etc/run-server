NODE_ENV=production \
# Runs nginx that enables the servers to run
\
##### uncomment to enable nginx #####
# cp ./nginx.conf <nginx-path> &&\
# pkill $(pgrep -f nginx) &&\
# nginx -c ./nginx.conf
\
cd ../ && node app.js 8000 &